<%= content_for :title, 'Dashboard' %>


<h2>Dashboard</h2>

<h4>my lyriks</h4>

<table>
  <tr>
    <th>Name</th>
    <th>User</th>
    <th>Rating</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @songs.each do |song| %>
  <tr>
    <td><%= song.name %></td>
    <td><%= song.user_id %></td>
    <td><%= song.rating %></td>
    <td><%= link_to 'Show', song %></td>
    <td><%= link_to 'Edit', edit_song_path(song) %></td>
    <td><%= link_to 'Destroy', song, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Song', new_song_path %>


<h4>Comments</h4>
<p>link to each of that person's comments.
</p>